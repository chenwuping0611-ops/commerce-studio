# 参考源码目录

本目录只存放经过确认的第三方参考源码，不作为生产运行时目录。

## 当前指定版本

```text
infinite-canvas v0.15.1
```

放置位置：

```text
references/infinite-canvas-v0.15.1/
```

用途：

- 学习无限画布和创作交互
- 分析节点、素材和 Prompt 操作
- 对比 Canvas 数据结构
- 选择可以重写或复用的前端能力

禁止：

- 直接把第三方源码当作本项目业务代码修改
- 提交第三方依赖安装目录
- 提交第三方项目的用户数据、API Key 或构建产物
- 未完成许可证审查就复制大段源码

实际业务代码仍然必须放在本项目的 `src/` 和 `apps/` 规划内，并遵守根目录 `agent.md`。
